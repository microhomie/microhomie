==========================
Microhomie PIR Sensor Node
==========================

Microhomie to detect motion with an PIR sensor. The sensor can be enabled and disabled with the `active` property. If enabled and a motion is deteced send a broadcast message.

